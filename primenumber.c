#include <stdio.h>

// Function declaration
void isPrime(int num);

int main() {
    int number;

    // Input from user
    printf("Enter a number: ");
    scanf("%d", &number);

    isPrime(number);

    return 0;
}

void isPrime(int num)
{
    int count=0;
    for()
}

