#include <stdio.h>

int main() {
    int num, i = 1;

    // Ask the user to enter an integer
    printf("Enter an integer: ");
    scanf("%d", &num);

    // Using conditions instead of a loop
    if (i == 1) {
        printf("%d * %d = %d\n", num, i, num * i);
        i++;
    }
    if (i == 2) {
        printf("%d * %d = %d\n", num, i, num * i);
        i++;
    }
    if (i == 3) {
        printf("%d * %d = %d\n", num, i, num * i);
        i++;
    }
    if (i == 4) {
        printf("%d * %d = %d\n", num, i, num * i);
        i++;
    }
    if (i == 5) {
        printf("%d * %d = %d\n", num, i, num * i);
        i++;
    }
    if (i == 6) {
        printf("%d * %d = %d\n", num, i, num * i);
        i++;
    }
    if (i == 7) {
        printf("%d * %d = %d\n", num, i, num * i);
        i++;
    }
    if (i == 8) {
        printf("%d * %d = %d\n", num, i, num * i);
        i++;
    }
    if (i == 9) {
        printf("%d * %d = %d\n", num, i, num * i);
        i++;
    }
    if (i == 10) {
        printf("%d * %d = %d\n", num, i, num * i);
    }

    return 0;
}

